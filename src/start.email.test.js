const SUT = require('./email')

describe('emailProcessing', () => {
  it('processes the email as expected', () => {
    // const actualResult = SUT.emailProcessing()
    // console.log(actualResult)
    expect(true).toBe(true)
  })
})
